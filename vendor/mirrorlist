# Rattan pinned mirrorlist — bootstrap rootfs provisioning only.
# Pin: 2026.08.01 — matched to archlinux-bootstrap-x86_64.tar.zst release.
# Only HTTPS mirrors; never accept HTTP or arbitrary URLs.
Server = https://geo.mirror.pkgbuild.com/$repo/os/$arch
Server = https://mirror.rackspace.com/archlinux/$repo/os/$arch
