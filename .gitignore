__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/
.py-site/
# LFS-tracked bootstrap tarball is kept in vendor/, but extracted rootfs never
# committed.
rootfs/
sessions/
layers/
# Built binaries (sources now in vendor/palisade/src/palisade/stage3.c and vendor/palisade/tests/probes/*.c).
# Note: bin/ is NOT ignored as a whole — bin/bootstrap-rootfs.sh is committed
# source; built stage3 and probes now live under vendor/palisade.
# cosmocc build intermediates (fat APE + multi-arch artifacts)
*.ape
*.com.dbg
*.aarch64.elf
*.ape.bak
bin/.aarch64/
bin/*.o
